import CartIcon from "./CartIcon";

const NavBar = () => {
  // Obtiene un valor del state
  return (
    <header>
      <h1>My store</h1>
      <CartIcon />
    </header>
  );
};

export default NavBar;
